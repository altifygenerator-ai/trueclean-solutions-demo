TrueClean flyer update

Drop these files into the project root and overwrite when prompted:

- src/app/page.tsx
- src/app/globals.css
- src/components/EquipmentFlyer.tsx
- public/images/equipment-cleaning-flyer.png

Placement:
The flyer has been added as a new Heavy Equipment Cleaning section on the homepage, placed after the Before & After section and before the Recent Work gallery.

Check:
ESLint passed. The production build could not complete in this sandbox because Next/font could not fetch Google Fonts without network access, not because of this update.
